<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.tech2020">

    <uses-permission android:name="android.permission.SEND_SMS"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/technovation_auxilium_logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".Hamburger"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".COVID19"/>
        <activity android:name=".Info"/>
        <activity android:name=".StreetDisastersShooting"/>
        <activity android:name=".Earthquake"/>
        <activity android:name=".Reward"/>
        <activity android:name=".Home" android:label="Auxillium"/>
        <activity android:name=".View_Profile" />
        <activity android:name=".User_Registration" />
        <activity android:name=".CategoryActivity" />
    </application>

</manifest>